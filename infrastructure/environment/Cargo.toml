[package]
name = "environment"
version = "0.1.0"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
application = { path = "../../application", version = "0.1.0" }
domain = { path = "../../domain", version = "0.1.0" }
