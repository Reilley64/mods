[package]
name = "projfs-game-view"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(windows)'.dependencies]
windows = { version = "=0.62.2", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_Storage_ProjectedFileSystem",
] }
